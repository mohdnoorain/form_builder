> Overview

The objective of this assignment is to build a full-stack dynamic form builder system.
The backend will expose a dynamic form schema and submission-related APIs.
The frontend will render the form dynamically, handle form submissions, and display prior
submissions in a paginated and sortable table.


> Milestone 1 (Frontend)

- Dynamic Form Page
   - GET /api/v1/form?uuid= ✅
   - Implemented loading and error states 
   - Fetch using TanStack Query 

